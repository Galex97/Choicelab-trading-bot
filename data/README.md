# Local market data

This repository does not distribute market-data files or vendor documentation.

If a script supports optional fundamentals, obtain the data directly from a source you are licensed to use and pass its local path with the `FUNDAMENTALS_CSV` environment variable. Do not commit that file or its accompanying documentation.
